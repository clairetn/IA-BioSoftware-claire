name: Python CI 🚀

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  setup:
    name: 🛠️ Setup
    runs-on: ubuntu-latest
    steps:
      - name: 🛒 Checkout code
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build dependencies
        run: sudo apt-get update && sudo apt-get install -y python3-dev build-essential

      - name: 📦 Install dependencies from pyproject.toml
        run: |
          python -m pip install --upgrade pip
          pip install .

  #   ruff:
  #     name: 🧐 Ruff Lint
  #     runs-on: ubuntu-latest
  #     needs: setup
  #     steps:
  #       - name: 🛒 Checkout code
  #         uses: actions/checkout@v4
  #
  #       - name: 🐍 Set up Python
  #         uses: actions/setup-python@v5
  #         with:
  #           python-version: "3.11"
  #
  #       - name: 📦 Install Ruff
  #         run: python -m pip install ruff
  #
  #       - name: 🔍 Run Ruff Lint
  #         run: ruff check . --force-exclude --exclude developpement_logiciel/burger.py
  #
  #       - name: 📝 Run Ruff Format Check
  #         run: ruff format --check . --force-exclude --exclude developpement_logiciel/burger.py

  #   safety:
  #     name: 🔒 Safety Check
  #     runs-on: ubuntu-latest
  #     needs: ruff
  #     steps:
  #       - name: 🛒 Checkout code
  #         uses: actions/checkout@v4
  #
  #       - name: 🐍 Set up Python
  #         uses: actions/setup-python@v5
  #         with:
  #           python-version: "3.11"
  #
  #       - name: 📦 Install dependencies
  #         run: |
  #           python -m pip install --upgrade pip
  #           pip install .
  #           pip install safety
  #
  #       - name: 🔒 Run Safety
  #         run: safety check

  #   bandit:
  #     name: 🕵️ Bandit Security Analysis
  #     runs-on: ubuntu-latest
  #     needs: safety
  #     steps:
  #       - name: 🛒 Checkout code
  #         uses: actions/checkout@v4
  #
  #       - name: 🐍 Set up Python
  #         uses: actions/setup-python@v5
  #         with:
  #           python-version: "3.11"
  #
  #       - name: 📦 Install Bandit
  #         run: python -m pip install bandit
  #
  #       - name: 🕵️ Run Bandit
  #         run: bandit -r . -x tests,developpement_logiciel/burger.py

  #   pytest:
  #     name: 🧪 Pytest with Coverage
  #     runs-on: ubuntu-latest
  #     needs: bandit
  #     steps:
  #       - name: 🛒 Checkout code
  #         uses: actions/checkout@v4
  #
  #       - name: 🐍 Set up Python
  #         uses: actions/setup-python@v5
  #         with:
  #           python-version: "3.11"
  #
  #       - name: 📦 Install package + test tools
  #         run: |
  #           python -m pip install --upgrade pip
  #           pip install .
  #           pip install pytest pytest-cov
  #
  #       - name: 🧪 Run Pytest with Coverage
  #         run: pytest --ignore=developpement_logiciel/burger.py --cov=./ --cov-report=term-missing

  build:
    name: 📦 Build distributions
    runs-on: ubuntu-latest
    needs: setup
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: 🛒 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install build tool
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: 🏗️ Build sdist and wheel
        run: python -m build

      - name: ⬆️ Upload dist artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

  release:
    name: 🚀 Automatic Release
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - name: 🛒 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install Python Semantic Release
        run: |
          python -m pip install --upgrade pip
          pip install python-semantic-release

      - name: 🚀 Run semantic release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: semantic-release version

  publish:
    name: 🚀 Publish to PyPI
    runs-on: ubuntu-latest
    needs: [build, release]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      id-token: write
      contents: read
    environment:
      name: pypi
      url: https://pypi.org/
    steps:
      - name: ⬇️ Download dist artifacts
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

      - name: 🚀 Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
